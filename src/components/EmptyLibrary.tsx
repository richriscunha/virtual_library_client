import React, {FunctionComponent} from "react";

const EmptyLibrary: FunctionComponent = () => {
  return (
    <div>
      <p>You don't have any books, click the button to add one.</p>
    </div>
  );
};

export default EmptyLibrary;
